L = ["Ram", 1, "Shyam", 2, "Aman", 3]
print("Original list:", L)

sorted_list = sorted(L, key=lambda x: str(x))
print("Sorted list:", sorted_list)

#The lambda function lambda x: str(x) takes an element x from the list
#and converts it to a string using the str() function.
#This ensures that all elements in the list are treated as strings during the sorting process
